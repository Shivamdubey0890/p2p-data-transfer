# Baked into every production build (Vercel, Render, Docker) so static hosts
# don't need their own env configuration. Points at the signaling server.
VITE_SERVER_URL=https://p2p-transfer-server.onrender.com
